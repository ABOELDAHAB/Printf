#ifndef MAIN_H
#define MAIN_H
#include <unistd.h>
#include <stdarg.h>
void deci(int b);
int _printf(const char *format, ...);
int pr(char c);
void str(char *s);
int _pprintf(const char *format, ...);

#endif
